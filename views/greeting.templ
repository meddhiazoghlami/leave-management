package views

templ Greeting(name string, renderedAt string) {
	<h1 class="mt-2 text-3xl font-bold text-slate-900">hello { name }</h1>
	<p class="mt-3 text-sm text-slate-500">Rendered at { renderedAt }</p>
}
